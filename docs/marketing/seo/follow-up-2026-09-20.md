# SEO follow-up — September 20, 2026

**Historical record:** the key-event default was subsequently corrected. See the
[September 21 readback and consent repair](follow-up-2026-09-21.md) for current status.

This record supersedes the registration and live-check status in the
[September 18 completion record](completion-2026-09-18.md). Implementation and
ongoing SEO improvement are approved. The next content improvement remains
behind the deployment and attribution gates.

## Verified settings and remaining discrepancy

The existing setup utility read property `543950329` without administrative
writes or changes to the reporting OAuth grant. It confirms `generate_lead`
exists with `ONCE_PER_EVENT`, and `cta_type`, `link_location`, and `form_location`
exist with EVENT scope. No registrations were duplicated.

The API reads at 07:12 and 07:24 UTC differ from the coordinator's reported
no-default selection: the existing key event still has `defaultValue` of
`numericValue: 1`, `currencyCode: USD`. This remains a discrepancy to resolve on
the existing event through authorized administration. The reporting grant still
has no Analytics edit scope; it remains unchanged. The utility now flags a
wrong counting method or any default value as `configuration_mismatch`, including
zero-valued defaults. `--check` exits 2; `--apply` performs no mutations when an
existing mismatch is present. It also verifies these settings after a creation.

GA browser-history pageviews are still enabled. Manual web pageviews remain off;
there was no change to GA stream settings or hosting flags. Complete the existing
coordinated GA-setting/web-rebuild procedure before enabling manual measurement.

## Hosting gate

The [September 20 deployment recheck](https://github.com/allgpt-co/QuickVoice/actions/runs/35496361843)
used the established workflow and receiver checks, with manual pageviews set to
preserve. It stopped at the first Coolify read with HTTP 403 and an explicit
API permission restriction, before any flag mutation or deployment request.
The configured repository secret's last update remains September 11. An
authorized hosting owner must supply appropriate access through secure settings;
no permission check was bypassed and no token was exposed.

The API revision, live receiver health/configuration, server-side attribution
activation and the marked real delivery/inbox/GA receipt test remain unverified.
The earlier successful backend workflow is a deployment acknowledgement, not
proof of those remaining facts.

## Live public and intercepted-browser checks

At 07:23 UTC all 101 sitemap URLs passed HTTP 200, indexable metadata, expected
apex canonical, one H1 and nonempty description checks: 34 static pages and
67 reviewed articles. Raw local evidence remains in ignored `output/seo/2026-09-20/`.

Live browser QA intercepted Analytics collection, session recording and contact
forwarding. It created no delivered enquiry and no received GA event:

- Initial load, SPA navigation, back, forward and reload each emitted one
  pageview with the expected URL and title after dispatch settled. Back/forward
  were observed over 6.5 seconds each. The stream is still using automatic views.
- Blank submission sent no request. A mocked failed response preserved the
  message and emitted zero lead events. A successful manual retry reused the
  same receipt and emitted one `generate_lead` event; the event request contained
  no submitted contact values or receipt identifiers.
- A separate intercepted failure after IVR-article-to-contact SPA navigation
  retained `/blog/ai-voice-agent-vs-ivr` as the browser-observed landing path and
  `contact_page` as the form location. Direct browser QA is not organic traffic
  or a qualified lead. A document reload correctly starts a new observation.
- The live form displays the privacy/source-context notice and a working policy
  link. No consent commands, consent checkbox or accept/reject interface was
  observed. GA and session-recording scripts attempt to load before interaction.
  This does not verify opt-in or rejection behavior. Required consent behavior
  needs a product/privacy-owner decision before implementation changes; no legal
  compliance conclusion is drawn from this check.

The intercepted successful response verifies client behavior only. Do not label
it provider acceptance, inbox arrival, server attribution activation or GA
collection. Preserve the single marked end-to-end enquiry for after the gates.

## Existing reporting and one prioritized improvement

Private report [35494188713](https://github.com/allgpt-co/QuickVoiceMarketing/actions/runs/35494188713)
passed all 141 requests. Portfolio health
[35495317206](https://github.com/allgpt-co/QuickVoiceMarketing/actions/runs/35495317206)
also succeeded, but its observation preceded the coordinator's GA registration
changes. The Monday private reporting workflow and the coordinator's monitoring
remain unchanged; no duplicate schedule or fresh full report was created.

The [private IVR brief](https://github.com/allgpt-co/QuickVoiceMarketing/blob/main/seo-operations/followups/2026-09-20-quickvoice.md)
contains the existing query evidence and one proposed clarification for
`/blog/ai-voice-agent-vs-ivr`: distinguish speech-routed IVR from a conversational
agent and test one representative call. It keeps the existing commercial/contact
journey, canonical and publication date. This is prepared, not published; refresh
primary sources and the exact-content review when the gates permit release.

DataForSEO's configured MCP is enabled and has credential fields present. No
secret values were printed. No explicit paid-call cap or cache setting was
found in that configuration, and no raw QuickVoice cache was located in the
inspected SEO output/private-reporting paths. Earlier research notes remain
historical context. No paid DataForSEO endpoint was called; current GSC evidence
is sufficient for this one prioritized improvement.

## Verification

The measurement utility's 21 tests pass; all 51 SEO-operations tests pass.
The added tests reject wrong counting methods, USD 1 and USD 0 defaults, and
incorrect post-creation readbacks without silently modifying existing settings.
The original grant-refresh test still ensures no new scopes are requested.

Reference: [GA4 key-event resource and optional default value](https://developers.google.com/analytics/devguides/config/admin/v1/rest/v1beta/properties.keyEvents).
